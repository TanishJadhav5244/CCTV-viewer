[project]
name = "cctv-analytics"
version = "2.0.0"
description = "CCTV Deep Learning Analytics — YOLOv8 + CLIP + FastAPI"
requires-python = ">=3.9"

[project.scripts]
app = "main:app"

[tool.uvicorn]
app = "main:app"
host = "0.0.0.0"
port = 8000
